
export const ADD_ITEM = 'ITEM_ADDED'
export const MARK_READ = 'MARK_READ'
export const MARK_UNREAD = 'MARK_UNREAD'

// feed_item_id, feed_name, url, title, author, body, leadImg

export const addItem = (item) => { return { type: ADD_ITEM, item }}
// export const markRead = (index) => { return { type: MARK_READ, index }}
// export const markUnread = (index) => { return { type: MARK_UNREAD, index }}
