# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

pod 'Firebase/Core', '~> 5.20.1'
pod 'Firebase/Auth', '~> 5.20.1'
pod 'Firebase/Database', '~> 5.20.1'
pod 'Firebase/Firestore', '~> 5.20.1'
pod 'GoogleSignIn', '~> 4.4.0'

target 'rizzle' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for rizzle

  # pod 'RNInAppBrowser', :path => '../node_modules/react-native-inappbrowser-reborn'

  pod 'RNGestureHandler', :path => '../node_modules/react-native-gesture-handler'

  pod 'RNReanimated', :path => '../node_modules/react-native-reanimated'

  pod 'ReactNativeDarkMode', :path => '../node_modules/react-native-dark-mode'

  target 'rizzleTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'rizzle-tvOS' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for rizzle-tvOS

  target 'rizzle-tvOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'RizzleShare' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for RizzleShare

end
